
server.port: 8080

spring:
  datasource:
    url: jdbc:myql://localhost:8080/academia?useTimezone=true&serverTimezone=UTC&useLegacyDate
    username: root
    password: 
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.format_sql: true

logging:
  level:
    org:
      hibernate:
        type: trace